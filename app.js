function setup(){
    createCanvas(500,500);
    background(0);
    //create a variable and loop it as long as it is greater than
    //-1, making it infinate
    for(let i = 0; i>-1; i++){
        //print to the console
        console.log(i);
    }
}